

# Van Time Tracker keep track of tasks in a warehouse


## To Start

1. Execute `npm install` once more.

### npm run start to run server

TT

RewriteBase /
RewriteRule ^index\.html$ - [L]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
```
